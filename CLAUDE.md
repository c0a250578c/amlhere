# Amlhere

思考・感情・行動を記録して成長するAIチャットアプリ。

## 構成
- `sub.py` — バックエンド全部（FastAPI / Gemini API / FAISS / SQLite）
- `index.html` — フロント（単一ファイル）
- デプロイ: Render（バックエンド）+ GitHub Pages（フロント）

## 作者について
- プログラミング初心者。学習目的で開発している。
- コードを「自分で読んで理解できる」ようになるのが目標。
- このチャット（claude.ai）とClaude Codeは別セッション。互いの作業内容は自動共有されない。

## 作業ルール
- 勝手にコードを編集しない。変更提案は必ず先に説明し、承認を得てから実行する。
- 作業を始める前に、対象ファイルの現在の状態（git diff / git log）を必ず確認する。
  他のセッション（claude.aiの会話など）で既に直されている可能性があるため。
- 修正案は「原因 → 修正内容 → なぜそれで直るのか（初心者向け）」の順で説明する。
- 専門用語は必ず（）で噛み砕いて補足する。
- 一度に大量の変更をしない。1つずつ小さく進める。
- 修正のたびに git commit する。（Comet Gravityではこれを怠って戻せなくなった経験あり）
- 日本語で応答する。タメ口・結論ファーストで。

## 検証ルール
- 検証を依頼するときは、番号付きチェックリスト（対象／確認項目／合格基準／報告形式）で渡す。
- 1つのAIの報告だけで「完了」と判断しない。可能なら自分の目でも確認する。
- AIが指示範囲外の問題を自発的に見つけて直した場合、内容を必ず自分で確認してから採用する。

## 作業ログ
大きな修正をしたら、下の表に1行追記する（日付／何を／なぜ／どう直したか）。

| 日付 | 対象 | 問題 | 対応 |
|---|---|---|---|
| 2026-08-07 | `sub.py` | `to_embedding` が未定義の `client` を参照していてGemini埋め込みが動かない | `gemini_client` に修正（cf00b21） |

## 既知の課題
1. レート制限が未実装（他人が使うとAPI費用が発生する）
2. 古い記憶の要約機能が未実装（プロンプトが際限なく伸びる）

## 解決済み
- `to_embedding` の変数名バグ（`client` → `gemini_client`）→ cf00b21 で修正済み
